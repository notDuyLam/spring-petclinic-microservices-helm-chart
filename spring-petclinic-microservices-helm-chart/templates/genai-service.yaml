apiVersion: apps/v1
kind: Deployment
metadata:
  name: spring-petclinic-genai-service-deployment
  labels:
    app: spring-petclinic-genai-service-deployment
  namespace: {{ .Values.namespace }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: spring-petclinic-genai-service-pod
  template:
    metadata:
      name: spring-petclinic-genai-service-pod
      labels:
        app: spring-petclinic-genai-service-pod
    spec:
      initContainers:
        - name: wait-for-config-server
          image: darthcabs/tiny-tools:1
          args:
            - /bin/bash
            - -c
            - >
              set -x;
              while [[ "$(curl -s -o /dev/null -w ''%{http_code}'' http://spring-petclinic-config-server-service:8888)" != "200" ]]; do
                echo '.'
                sleep 15;
              done
        - name: wait-for-discovery-server
          image: darthcabs/tiny-tools:1
          args:
            - /bin/bash
            - -c
            - >
              set -x;
              while [[ "$(curl -s -o /dev/null -w ''%{http_code}'' http://spring-petclinic-discovery-server-service:8761)" != "200" ]]; do
                echo '.'
                sleep 15;
              done
      containers:
        - name: spring-petclinic-genai-service-pod
          image: "{{ .Values.images.genaiService.repository }}/{{ .Values.images.genaiService.name }}:{{ .Values.images.genaiService.tag }}"
          imagePullPolicy: IfNotPresent
          env:
            - name: OPENAI_API_KEY
              value: ""
            - name: AZURE_OPENAI_KEY
              value: ""
            - name: AZURE_OPENAI_ENDPOINT
              value: ""
            - name: CONFIG_SERVER_URL
              value: "http://spring-petclinic-config-server-service:8888"
            - name: DISCOVERY_SERVER_URL
              value: "http://spring-petclinic-discovery-server-service:8761/eureka/"
            - name: TRACING_SERVER_URL
              value: "http://spring-petclinic-tracing-server-service:9411/api/v2/spans"
          ports:
            - containerPort: 8084
---
apiVersion: v1
kind: Service
metadata:
  name: spring-petclinic-genai-service-service
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: spring-petclinic-genai-service-pod
  ports:
    - name: spring-petclinic-genai-service-port
      protocol: TCP
      port: 8084
      targetPort: 8084
